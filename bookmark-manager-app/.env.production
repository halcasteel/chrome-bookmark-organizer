# Production Environment Configuration

# Application
NODE_ENV=production
PORT=3001

# Database (Cloud SQL)
# This will be set in Cloud Run environment variables
# DATABASE_URL=postgresql://username:password@/database_name?host=/cloudsql/project:region:instance

# Security
# JWT_SECRET should be set in Cloud Run environment

# OpenAI (for semantic search)
# OPENAI_API_KEY should be set in Cloud Run environment

# File Import
BOOKMARK_IMPORT_DIR=/tmp/imports

# Google Cloud Platform
GCP_PROJECT_ID=bookmarks-manager-462919
GCP_PROJECT_NUMBER=999739320361
GCP_REGION=us-central1
CLOUD_SQL_CONNECTION_NAME=bookmarks-manager-462919:us-central1:bookmark-manager-db
GCS_BUCKET_NAME=bookmarks-manager-462919-bookmarks

# Frontend (set in Cloud Run)
VITE_API_URL=https://bookmark-manager-backend-us-central1-uc.a.run.app/api

# CORS
CORS_ORIGIN=https://bookmarks.az1.ai
FRONTEND_URL=https://bookmarks.az1.ai